# =============================================
# Metadata Service — Kubernetes Deployment
# =============================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: metadata-service
  namespace: lakehouse-platform
  labels:
    app: metadata-service
    app.kubernetes.io/part-of: lakehouse-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: metadata-service
  template:
    metadata:
      labels:
        app: metadata-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: metadata-service
          image: registry.internal/lakehouse/metadata-service:latest
          ports:
            - containerPort: 8000
              name: http
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          envFrom:
            - secretRef:
                name: lakehouse-secrets
            - configMapRef:
                name: lakehouse-config
---
apiVersion: v1
kind: Service
metadata:
  name: metadata-service
  namespace: lakehouse-platform
spec:
  selector:
    app: metadata-service
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  type: ClusterIP

